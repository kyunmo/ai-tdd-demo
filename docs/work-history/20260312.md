# 작업기록 2026-03-12

---
## 테스트 코드 생성 내역

- **UserService 테스트 생성**
- **NoticeService 테스트 생성**
- **AuthService 테스트 생성**

---
## 대화내역(프롬프트) 요약

LLM 모델 변경: `gpt-oss-120b` → `glm-5-fp8`

### 1회차 : 테스트 파일 삭제 & 클로드코드 세션 재생성 후 실행

> ai-tdd 에이전트 활용해서 UserService 테스트 파일 생성 해줘
```
● UserService 테스트 파일 생성이 완료되었습니다.

  생성 결과

  생성 파일: src\test\java\com\nhcard\al\demo\service\UserServiceTest.java

  테스트 구성

  ┌─────────────────────┬───────────┬─────────────────────────┐
  │        레벨         │ 테스트 수 │          설명           │
  ├─────────────────────┼───────────┼─────────────────────────┤
  │ Level 1: Happy Case │ 6개       │ 정상 흐름 테스트        │
  ├─────────────────────┼───────────┼─────────────────────────┤
  │ Level 2: Edge Case  │ 7개       │ 경계값/특수 상황 테스트 │
  ├─────────────────────┼───────────┼─────────────────────────┤
  │ Level 3: Exception  │ 4개       │ 예외 상황 테스트        │
  ├─────────────────────┼───────────┼─────────────────────────┤
  │ Level 4: Mutation   │ 4개       │ 로직 검증 테스트        │
  └─────────────────────┴───────────┴─────────────────────────┘

  총 21개 테스트가 작성되었으며 모두 PASS 되었습니다.

  적용된 NH 도메인 규칙

  - ✅ PII 마스킹 검증 (전화번호 마스킹 결과 검증)
  - ✅ 테스트 데이터 보안 (더미 데이터 사용)

  테스트 코드 특징

  - Given-When-Then 패턴 적용
  - AssertJ, Mockito 활용
  - 한글 @DisplayName 적용
  - @Nested 구조로 테스트 그룹화
```

**결과**:이전 gpt-oss보다 템플릿에 더 정확하게 생성됨 
1. Given-When-Then 패턴: 모든 테스트 메서드에 적용
2. AssertJ 사용: assertThat(), assertThatThrownBy() 활용
3. Mockito 검증: verify(), never(), times(), InOrder 활용
4. 한글 @DisplayName: 모든 테스트에 시나리오 설명
5. @Nested 구조: Edge/Exception/Mutation 케이스를 그룹화
6. 마스킹 검증: 전화번호 마스킹 결과 검증 (doesNotContain("1234"))


claude 재실행 (신규 session)
> NoticeService 테스트 코드 생성해줘
```
● NoticeService 테스트 코드를 생성했습니다.
                                                                                                                                                                                                                                                                      생성된 테스트 코드 구조:                                                                                                                                                                                                                                                                  
  ┌───────────────┬──────────────────────────────────────┐                                                                                                                                                                                                          
  │    메서드     │            테스트 케이스             │
  ├───────────────┼──────────────────────────────────────┤
  │ getNoticeById │ 정상 조회, 존재하지 않는 ID 예외     │
  ├───────────────┼──────────────────────────────────────┤
  │ getAllNotices │ 목록 조회, 빈 목록 조회              │
  ├───────────────┼──────────────────────────────────────┤
  │ createNotice  │ 공지사항 생성                        │
  ├───────────────┼──────────────────────────────────────┤
  │ updateNotice  │ 공지사항 수정, 존재하지 않는 ID 예외 │
  ├───────────────┼──────────────────────────────────────┤
  │ deleteNotice  │ 공지사항 삭제, 존재하지 않는 ID 예외 │
  └───────────────┴──────────────────────────────────────┘

  테스트 파일 위치:
  src/test/java/com/nhcard/al/demo/service/NoticeServiceTest.java

  사용 기술:
  - JUnit 5
  - Mockito
  - AssertJ

  Maven이 환경에 설치되어 있지 않아 테스트 실행은 불가능했습니다. IDE에서 직접 실행하거나 Maven 환경 설정 후 실행해 주세요.
```
**결과**:
- 에이전트 활용하지 않음
- 메서드 단위로 테스트 생성됨 -> 9개 테스트코드 (Happy + Exception)

> ai-tdd 이용해서 authService 테스트 코드 생성해줘
```
● AuthService 테스트 코드 생성이 완료되었습니다.

  생성 결과 요약

  생성 파일: src/test/java/com/nhcard/al/demo/service/AuthServiceTest.java

  테스트 구성 (총 10개)

  ┌────────────┬───────────┬──────────────────────────────────────────────────────────┐
  │    레벨    │ 테스트 수 │                           설명                           │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Happy Case │ 2개       │ 정상 로그인, 정상 회원가입                               │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Edge Case  │ 3개       │ 존재하지 않는 이메일, 잘못된 비밀번호, 중복 이메일       │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Exception  │ 3개       │ 사용자 없음 예외, 비밀번호 불일치 예외, 중복 이메일 예외 │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Mutation   │ 2개       │ 비밀번호 암호화 검증, JWT 토큰 생성 검증                 │
  └────────────┴───────────┴──────────────────────────────────────────────────────────┘

  주요 검증 포인트

  - passwordEncoder.encode() 호출로 비밀번호 암호화 확인 (Petra 암호화 규칙)
  - jwtTokenProvider.generateToken() 호출로 토큰 생성 확인
  - ArgumentCaptor로 평문 비밀번호가 저장되지 않음을 검증
  - InOrder로 메서드 호출 순서 검증

  실행 결과

  컴파일: PASS
  테스트 실행: PASS (10개 성공)

```

NoticeServiceTest.java 삭제
> ai-tdd 이용 NoticeService 테스트 코드 생성
```
● NoticeService 테스트 코드 생성이 완료되었습니다.

  생성 결과 요약

  생성 파일: src/test/java/com/nhcard/al/demo/service/NoticeServiceTest.java

  테스트 구성 (총 15개)

  ┌────────────┬───────────┬──────────────────────────────────────────┐
  │    레벨    │ 테스트 수 │                   설명                   │
  ├────────────┼───────────┼──────────────────────────────────────────┤
  │ Happy Case │ 5개       │ 정상 조회, 전체 목록, 생성, 수정, 삭제   │
  ├────────────┼───────────┼──────────────────────────────────────────┤
  │ Edge Case  │ 5개       │ 빈 리스트, ID=0, ID 음수 값              │
  ├────────────┼───────────┼──────────────────────────────────────────┤
  │ Exception  │ 3개       │ NoticeNotFoundException (조회/수정/삭제) │
  ├────────────┼───────────┼──────────────────────────────────────────┤
  │ Mutation   │ 2개       │ 메서드 호출 순서 검증, 호출 횟수 검증    │
  └────────────┴───────────┴──────────────────────────────────────────┘

  주요 검증 포인트

  - getNoticeById: 조회 후 incrementViewCount 호출 검증 (InOrder)
  - createNotice: ArgumentCaptor로 insert 인자 검증
  - 예외 상황에서 NoticeNotFoundException 발생 확인
  - verify(times(1)), never()로 정밀한 호출 검증

  실행 결과

  컴파일: PASS
  테스트 실행: PASS (15개 성공)
```


---
## 전체 평가 (AI에게 테스트 소스 검토 후 평가 요청)
```
1. 장점 분석

공통 장점

- Given-When-Then 패턴 일관성 있게 적용
- AssertJ 어설션으로 가독성 좋은 검증
- @Nested 클래스로 테스트 그룹화 체계적
- InOrder 검증으로 메서드 호출 순서 보장
- never() 검증으로 불필요한 호출 차단
- @DisplayName 한글 설명으로 비즈니스 의도 명확

AuthServiceTest (10/10)

- ArgumentCaptor로 평문 비밀번호 저장 방지 검증 (보안)
- 로그인 흐름: 이메일 조회 → 비밀번호 검증 → 토큰 생성 순서 검증
- 예외 발생 시 후속 작업(encode, insert) 미호출 검증

NoticeServiceTest (9/10)

- 모든 CRUD 메서드에 Happy Case 커버
- incrementViewCount 호출 순서 검증 (조회수 증가)
- ArgumentCaptor로 insert 인자 검증

UserServiceTest (10/10)

- 전화번호 마스킹 NH 도메인 규칙 검증 포함
- ID 입력값 검증 (null, 0, 음수) 포함
- 가장 풍부한 Mutation 테스트 (4개)
- Helper Method로 테스트 데이터 생성 표준화

  ---
4. 코드 품질 점수

┌─────────────────┬─────────────────┬───────────────────┬──────────────────┐
│      항목       │ AuthServiceTest │ NoticeServiceTest │ UserServiceTest  │
├─────────────────┼─────────────────┼───────────────────┼──────────────────┤
│ 테스트 커버리지 │ ⭐⭐⭐⭐ (80%)  │ ⭐⭐⭐⭐ (85%)    │ ⭐⭐⭐⭐⭐ (95%) │
├─────────────────┼─────────────────┼───────────────────┼──────────────────┤
│ 코드 가독성     │ ⭐⭐⭐⭐⭐      │ ⭐⭐⭐⭐⭐        │ ⭐⭐⭐⭐⭐       │
├─────────────────┼─────────────────┼───────────────────┼──────────────────┤
│ 중복 제거       │ ⭐⭐⭐          │ ⭐⭐⭐            │ ⭐⭐⭐⭐         │
├─────────────────┼─────────────────┼───────────────────┼──────────────────┤
│ NH 규칙 준수    │ ⭐⭐⭐⭐        │ ⭐⭐⭐            │ ⭐⭐⭐⭐⭐       │
├─────────────────┼─────────────────┼───────────────────┼──────────────────┤
│ 종합 점수       │ 82점            │ 83점              │ 90점             │
└─────────────────┴─────────────────┴───────────────────┴──────────────────┘
```

> ai-tdd NoticeController 테스트 코드 생성

```
● 먼저 NoticeController의 내용을 확인하겠습니다.

● Searched for 1 pattern, read 1 file (ctrl+o to expand)

● NoticeController 내용을 확인했습니다. ai-tdd 에이전트로 테스트 코드를 생성하겠습니다.

● ai-tdd(NoticeController 테스트 코드 생성)
  ⎿ Done (60 tool uses · 0 tokens · 12m 47s)
  (ctrl+o to expand)

● 토큰 제한으로 오류가 발생했습니다. 더 간결하게 다시 시도하겠습니다.

● ai-tdd(NoticeController 테스트 생성)
  ⎿ Done (25 tool uses · 57.3k tokens · 3m 51s)
  (ctrl+o to expand)                    

● NoticeController 테스트 코드 생성이 완료되었습니다.                                                                                                                                                                                                                 
  생성 결과 요약                                                                                                                                                                                                                                                                                             
  생성 파일: src/test/java/com/nhcard/al/demo/controller/NoticeControllerTest.java                                                                                                                                                                                 
                                                                         
  테스트 구성 (총 15개) 
                                                                         
  ┌────────────┬───────────┬──────────────────────────────────────────────────────────┐
  │    레벨    │ 테스트 수 │                           설명                           │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Happy Case │ 5개       │ 전체 목록, 단건 조회, 생성, 수정, 삭제                   │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Edge Case  │ 5개       │ 빈 리스트, 유효성 검증 실패 (title/content/author blank) │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Exception  │ 3개       │ NoticeNotFoundException (조회/수정/삭제)                 │
  ├────────────┼───────────┼──────────────────────────────────────────────────────────┤
  │ Mutation   │ 2개       │ 요청 데이터 검증, 정확한 ID 검증                         │
  └────────────┴───────────┴──────────────────────────────────────────────────────────┘

  검증 결과

  컴파일: PASS
  테스트 실행: 12개 통과, 3개 스킵 (@Disabled)

  ⚠️ 소스 코드 버그 발견

  NoticeController.java:43-48에 버그가 있습니다:

  @PostMapping("/update")  // ← {id} 경로 변수 없음
  public ResponseEntity<Notice> updateNotice(
      @PathVariable Long id,  // ← @PathVariable인데 URL에 {id} 없음
      ...

  수정 권장: @PostMapping("/update/{id}")로 변경하거나, ID를 RequestBody에 포함시켜야 합니다.
```
**결과**:
- 소스코드 오류 발견 > 수정 권장

---
## 도출된 개선사항
- [ ] `ai-tdd 에이전트` 명시적으로 사용하면 결과값이 더 좋음
- [x] `Maven`환경을 찾는 경우가 생겨서 .claude파일에 의존성 관리도구를 추가
- [ ]