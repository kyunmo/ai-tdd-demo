# 문서 재검토 리포트 (2차) — 2026-03-17

> 1차 검토(report1) 수정 완료 후 전체 문서 재검토 결과
> 대상: `docs/ai-tdd-skills/` 16개 파일 + `.claude/agents/` 2개 파일

---

## 심각도 기준

| 심각도 | 의미 |
|---|---|
| **Critical** | 생성된 코드가 컴파일/실행 실패하는 오류 |
| **High** | 코드 예시 오류, 잘못된 API 사용 |
| **Medium** | 오타, 문법 오류, 표현 불일치 |
| **Low** | 경미한 표기, 스타일 이슈 |

---

## Critical (7건) — Java 8 호환성 위반

### C-1. `List.of()` 사용 (Java 9+)

| 파일 | 라인 | 현재 코드 |
|---|---|---|
| `templates/mapper-test.md` | 139 | `when(mapper.findAll()).thenReturn(List.of(...))` |
| `templates/mapper-test.md` | 160 | `thenReturn(List.of(entity1, entity2))` |
| `verification/compile-check.md` | 155 | `when(userMapper.findAll()).thenReturn(List.of(user1, user2));` |

**수정**: `Arrays.asList(...)` 또는 `Collections.singletonList(...)` 사용

### C-2. `var` 키워드 사용 (Java 10+)

| 파일 | 라인 | 현재 코드 |
|---|---|---|
| `templates/util-test.md` | 130 | `var constructor = {유틸클래스}.class.getDeclaredConstructor();` |
| `templates/util-test.md` | 144 | `var constructor = {유틸클래스}.class.getDeclaredConstructor();` |
| `references/examples/util-test-example.md` | 194 | `var constructor = MaskingUtil.class.getDeclaredConstructor();` |

**수정**: `Constructor<?> constructor = ...` 사용

### C-3. `Map.of()` 사용 (Java 9+)

| 파일 | 라인 | 현재 코드 |
|---|---|---|
| `references/examples/controller-test-example.md` | 51 | `return ResponseEntity.ok(Map.of("maskedPhoneNumber", masked));` |

**수정**: `Collections.singletonMap("maskedPhoneNumber", masked)` 사용

---

## High (8건) — 코드 예시 오류

### H-1. `when()` 괄호 누락 — `verification/test-execution.md`

**Line 134, 138** — 닫는 괄호 위치 오류로 컴파일 실패
```
- when(userMapper.findById(anyString()).thenReturn(user);
+ when(userMapper.findById(anyString())).thenReturn(user);
```

### H-2. 메서드명 공백 오류 — `verification/coverage-report.md`

**Line 135** — 메서드명에 공백이 있어 컴파일 실패
```
- void should_throwException_when userIsInactive() {
+ void should_throwException_when_userIsInactive() {
```

### H-3. `String` 타입을 값으로 사용 — `verification/coverage-report.md`

**Line 139, 142** — `String`은 타입이지 값이 아님
```
- when(userMapper.findById(String)).thenReturn(inactiveUser);
+ when(userMapper.findById(anyString())).thenReturn(inactiveUser);
```
```
- assertThatThrownBy(() -> userService.getUserById(String))
+ assertThatThrownBy(() -> userService.getUserById("testId"))
```

### H-4. `if` 조건문 괄호 누락 — `verification/coverage-report.md`

**Line 129** — 닫는 괄호 누락
```
- // if (user.getStatus() == Status.ACTIVE { ... }
+ // if (user.getStatus() == Status.ACTIVE) { ... }
```

### H-5. `Mediatype` → `MediaType` — `constraints/code-style.md`

**Line 208** — 대소문자 오류
```
- .contentType(Mediatype.APPLICATION_JSON))
+ .contentType(MediaType.APPLICATION_JSON))
```

### H-6. HTTP 상태코드 불일치 — `templates/controller-test.md`

**Line 261** — `isOk()`는 200인데 괄호 안에 (204)로 표기
```
- | `@DeleteMapping` + 반환값 있음 | `status().isOk()` (204) |
+ | `@DeleteMapping` + 반환값 있음 | `status().isOk()` (200) |
```

### H-7. 클래스명/패키지 오류 미수정 — `constraints/code-style.md`

**Line 195-196** — 1차 수정에서 패키지는 변경되었으나 클래스명·하위패키지 미수정
```
- import com.nhcard.al.demo.service.UserServices;
- import com.nhcard.al.demo.service.mapper.UserMapper;
+ import com.nhcard.al.demo.service.UserService;
+ import com.nhcard.al.demo.mapper.UserMapper;
```
(`UserServices` 복수형 → `UserService` 단수형, `service.mapper` → `mapper` 하위패키지 수정)

### H-8. `void*서드` 깨진 텍스트 — `templates/service-test.md`

**Line 196** — 플레이스홀더 텍스트 깨짐
```
- {인스턴스}.{void*서드}({인자});
+ {인스턴스}.{void메서드}({인자});
```

---

## Medium (9건) — 오타/문법

### M-1. `Given-When-then` → `Given-When-Then` — `constraints/code-style.md`

**Line 12** — Then 대문자 누락
```
- 모든 테스트는 **Given-When-then** 패턴으로 구조화합니다.
+ 모든 테스트는 **Given-When-Then** 패턴으로 구조화합니다.
```

### M-2. `예외 어셜션` → `예외 어설션` — `constraints/code-style.md`

**Line 134** — 한글 오타
```
- ### 2.2. 예외 어셜션
+ ### 2.2. 예외 어설션
```

### M-3. `Junit 5` → `JUnit 5` — `templates/controller-test.md`

**Line 11** — 대소문자
```
- Junit 5, MockMvc, Mockito, AssertJ를 사용하여
+ JUnit 5, MockMvc, Mockito, AssertJ를 사용하여
```

### M-4. `엔지니어 입니다` → `엔지니어입니다` — `templates/controller-test.md`

**Line 10** — 띄어쓰기
```
- 시니어 Java 테스트 엔지니어 입니다.
+ 시니어 Java 테스트 엔지니어입니다.
```

### M-5. `추가 않도록` → `추가되지 않도록` — `templates/service-test.md`

**Line 15** — 문법 오류
```
- 필요한 import 요소가 누락하거나 불필요하게 추가 않도록
+ 필요한 import 요소가 누락되거나 불필요하게 추가되지 않도록
```

### M-6. `(예:UserService)` → `(예: UserService)` — `.claude/agents/test-generator.md`

**Line 90** — 콜론 뒤 공백 누락
```
- 입력: {ClassName} (예:UserService)
+ 입력: {ClassName} (예: UserService)
```

### M-7. `제대조` → `재대조` — `.claude/agents/test-generator.md`

**Line 369** — 오타
```
- 메서드 시그니처를 소스 코드와 제대조하여 수정
+ 메서드 시그니처를 소스 코드와 재대조하여 수정
```

### M-8. `정의 합니다` → `정의합니다` — `constraints/naming-conventions.md`

**Line 3** — 띄어쓰기
```
- 이 문서는 테스트 코드 생성 시 적용하는 **네이밍 규칙**을 정의 합니다.
+ 이 문서는 테스트 코드 생성 시 적용하는 **네이밍 규칙**을 정의합니다.
```

### M-9. `--rerun` 무효 플래그 — `verification/test-execution.md`

**Line 31** — Gradle에 `--rerun` 옵션 없음
```
- ./gradlew test --rerun
+ ./gradlew test --rerun-tasks
```
(또는 해당 섹션 제거/수정)

---

## Low (3건) — 경미한 이슈

### L-1. PIT targetClasses 패턴 — `constraints/test-coverage.md`

**Line 95-96** — 와일드카드 범위가 너무 넓음 (프로젝트 패키지에 맞게 조정 필요)
```
targetClasses = ['com.nhcard.*']
→ 프로젝트 패키지에 맞게 조정 안내 주석 추가 권장
```

### L-2. 마침표 누락 — `constraints/nh-rules.md`

**Line 5** — 문장 끝 마침표 없음
```
- 커버리지 기준은 `test-coverage.md`를 참조하세요
+ 커버리지 기준은 `test-coverage.md`를 참조하세요.
```

### L-3. service-test-example.md 비밀번호 검증 로직

**Line 127** — `result.getPassword()`가 인코딩된 비밀번호인데 평문과 비교
```
assertThat(result.getPassword()).isEqualTo(TEST_PASSWORD);
```
→ 소스 코드에서 `passwordEncoder.encode()` 호출 여부에 따라 기대값이 다를 수 있음 (Mock 설정에 따라 올바를 수도 있음 — 확인 필요)

---

## 요약

| 심각도 | 건수 | 주요 내용 |
|---|---|---|
| Critical | 7 | Java 9+/10+ API 사용 (`List.of`, `var`, `Map.of`) |
| High | 8 | 괄호 누락, 메서드명 오류, 타입/값 혼동, 상태코드 불일치 |
| Medium | 9 | 한글 오타, 띄어쓰기, 대소문자, 무효 Gradle 플래그 |
| Low | 3 | 와일드카드 범위, 마침표, 로직 확인 필요 |
| **합계** | **27** | |

### 영향 파일 수: 11개

| 파일 | C | H | M | L |
|---|---|---|---|---|
| `templates/mapper-test.md` | 2 | | | |
| `templates/util-test.md` | 2 | | | |
| `templates/service-test.md` | | 1 | 1 | |
| `templates/controller-test.md` | | 1 | 2 | |
| `references/examples/util-test-example.md` | 1 | | | |
| `references/examples/controller-test-example.md` | 1 | | | |
| `references/examples/service-test-example.md` | | | | 1 |
| `verification/compile-check.md` | 1 | | | |
| `verification/test-execution.md` | | 1 | 1 | |
| `verification/coverage-report.md` | | 3 | | |
| `constraints/code-style.md` | | 2 | 2 | |
| `constraints/naming-conventions.md` | | | 1 | |
| `constraints/nh-rules.md` | | | | 1 |
| `constraints/test-coverage.md` | | | | 1 |
| `.claude/agents/test-generator.md` | | | 2 | |
