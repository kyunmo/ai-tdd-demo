# 작업기록 2026-03-11

---
## 테스트 코드 생성 내역
- **UserService 테스트 생성**
    - `src/test/java/com/nhcard/al/demo/service/UserServiceTest.java` 파일에 JUnit5, Mockito, AssertJ 기반 테스트 작성
    - Happy, Edge, Exception, Mutation 레벨 커버리지 적용
    - NH 도메인 규칙(마스킹) 적용 테스트 포함

- **NoticeService 테스트 생성**
    - `src/test/java/com/nhcard/al/demo/service/NoticeServiceTest.java` 파일에 동일한 레벨 테스트 작성
    - 조회수 증가, 예외 상황 등 전반적인 동작 검증

- **AuthService 테스트 생성**
    - `src/test/java/com/nhcard/al/demo/service/AuthServiceTest.java` 파일에 로그인/회원가입 로직 테스트 작성
    - 비밀번호 검증, 토큰 발급, 중복 이메일, 예외 상황 및 뮤테이션 테스트 포함

---
## 대화내역(프롬프트) 요약

### 1회차 : 테스트 파일 삭제 & 클로드코드 세션 재생성 후 실행

> ai-tdd 에이전트 이용해서 UserService.java 테스트 생성
```
● UserService.java 파일의 전체 내용을 제공해 주시겠어요? 테스트를
생성하려면 코드가 필요합니다.
```
- **결과** : 코드의 전체 또는 path경로 선택 사항 제공 -> path경로 선택 -> 소스 생성 완료
- 47.2k 토큰 소모, 13m 6s동안 작업

> src/main/java/com/nhcard/al/demo/service/NoticeService 테스트 코드 생성
```
화면에 코드 출력
```
- **결과** : 1차 파일생성 안됨 -> 재요청 파일 생성
- 테스트 fail 발생

> AuthService 테스트 파일 생성해줘 
```
 AuthService.java 파일의 전체 내용을 제공해 주시겠어요? 테스트를 생성하려면 정확한 메서드 시그니처가 필요합니다.

❯ 1. Paste content
    전체 소스 코드를 붙여넣기
  2. Provide path
     파일 경로만 알려 주세요
  3. Type something.

3번 선택후 경로 입력

● AuthService에 대한 테스트 파일이 src/test/java/com/nhcard/al/demo/service/AuthServiceTest.java 경로에 생성되었습니다.
  테스트는 Happy, Edge, Exception, Mutation 레벨을 모두 포함하고 있으며, NH 도메인 규칙(암호화, 감사 로그 등)도 반영되었습니다.
```
- **결과** : 정상 처리


### 2회차 : 스킬 문서 수정, 테스트 파일 삭제 & 클로드코드 세션 재생성 후 실행

> AuthService 테스트 파일 생성
```
요약: 파일 생성
```
- **결과** : 규칙에 어긋난 파일 생성
- 원인분석을 위해 추가 질의

> 왜 앞에 생성한 UserServiceTest와 NoticeServiceTest와 규칙 적용이 다른가?

```
● 왜 AuthServiceTest 만 규칙을 어겼나요?
  항목: 생성 방법
  UserServiceTest·NoticeServiceTest: ai‑tdd 에이전트를 두 번 호출하면서 플랜 → 검증 → 파일 작성 흐름을 거쳤습니다. 에이전트가 파일을 읽은 뒤 Write 를 수행했기 때문에, 기존 파일에 존재하는 import 목록을 자동으로 복제하고, @DisplayName을 영어(템플릿에 지정된 기본값)으로 만들었습니다.
  AuthServiceTest: ai‑tdd 에이전트를 한 번 호출했습니다. 프롬프트에 “한글 @DisplayName” 이라는 추가 요구가 포함되지 않았고, 파일을 읽지 않은 상태에서 바로 Write를 수행했기 때문에 · PasswordEncoder import를 누락했고 · 대화 언어가 한국어였기 때문에 에이전트가 **자동으로 한글 @DisplayName**을 생성했습니다.
  ────────────────────────────────────────
  항목: 규칙 적용 시점
  UserServiceTest·NoticeServiceTest: 파일을 읽고 Edit / Write 하기 전에 규칙 검증(불필요한 import 제거, 라벨 언어 검사)을 수행했습니다.
  AuthServiceTest: 파일을 읽지 않은 채 Write에 바로 넘겼으므로 검증 단계가 없었습니다.
  ────────────────────────────────────────
  항목: 템플릿 차이
  UserServiceTest·NoticeServiceTest: UserServiceTest·NoticeServiceTest 는 템플릿에 @DisplayName을 영어로 작성하도록 명시돼 있었고, 필요한 모든 Mock 타입(예: UserMapper)에 대한 import를 자동으로 삽입했습니다.
  AuthServiceTest: AuthServiceTest 템플릿에서는 언어를 지정하지 않았고 PasswordEncoder 같은 새 Mock 타입을 자동 import에 포함시키는 로직이 빠져 있었습니다.
```

> 영문 displayName이 규칙이 명시가 안되어 있는데 왜 영문으로 작성을 시도 했는지?

```
 1️⃣실제 규칙 문서에 있는 내용

  ┌──────────────────────────────────────────────────────┬────────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────┐
  │                         문서                         │                      규칙(관련 조항)                       │                          비고                           │
  ├──────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────┤
  │ docs/ai-tdd-skills/constraints/code‑style.md         │ 코드 스타일 – 들여쓰기, 라인 길이 등                       │ @DisplayName 언어에 대한 명시 없음                      │
  ├──────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────┤
  │ docs/ai-tdd-skills/constraints/naming‑conventions.md │ 클래스·메서드·변수 명명 – Java 표준을 따름                 │ 테스트 메서드 이름·@DisplayName 언어에 대한 규칙은 없음 │
  ├──────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────┤
  │ docs/ai-tdd-skills/constraints/nh‑rules.md           │ NH 도메인 규칙 – 마스킹·감사 로그·암호화 등                │ UI/라벨 언어와는 무관                                   │
  ├──────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────┤
  │ docs/ai-tdd-skills/templates/service‑test.md         │ 테스트 템플릿 – 4단계 레벨, JUnit5, AssertJ, Mockito 사용 │ @DisplayName 예시가 한글·영어 어느 쪽도 강제하지 않음   │
  ├──────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────┤
  │ docs/ai-tdd-skills/templates/controller‑test.md 등   │ 다른 템플릿 – 동일하게 언어 제한 없음                      │                                                         │
  └──────────────────────────────────────────────────────┴────────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────┘

  5️⃣결론 및 향후 약속

  1. 규칙은 문서에 명시된 것만 적용합니다. 현재는 @DisplayName 언어 제한이 없습니다.
  2. 규칙이 불분명하거나 누락된 경우엔 EnterPlanMode 로 사용자에게 확인을 거칩니다.
  3. 파일 생성 전 검증 단계와 컴파일·테스트 자동 실행을 반드시 포함해, 누락·오류를 사전에 차단합니다.
  4. 앞으로는 ‘규칙 → 검증 → 작성 → 확인’ 순서를 엄격히 따르겠습니다.
```

---

## 도출된 개선사항 (적용여부 결정)
- [x] @Nested 구조 정리 > 가독성이 개선
- [ ] 서비스 메서드에서 null 파라미터 검증 로직 추가
- [ ] AuthService 로그인 로직에서 비밀번호 암호화 로그 제거 (보안 강화)
- [ ] 테스트 코드 중복 설정을 @BeforeEach와 @Nested를 활용해 정리
- [x] 각 테스트 클래스에 @DisplayName 추가하여 테스트 그룹 가시성 향상
- [x] 언어에 대한 규칙으르 구체화

## 전체 평가 (AI에게 테스트 소스 검토 후 평가 요청)
- **테스트 완성도**: Happy, Edge, Exception, Mutation 네 가지 레벨을 모두 포함하여 비즈니스 로직과 예외 흐름을 충분히 검증함.
- **커버리지**: 라인 커버리지 92%·분기 커버리지 84%로 프로젝트 기준(라인 80%·분기 70%)을 초과함.
- **NH 도메인 규칙 적용**: 마스킹, 비밀번호 암호화, 감사 로그 등 핵심 보안 규칙을 테스트에 반영함.
- **코드 품질**: 테스트가 명확한 명명 규칙과 AssertJ 기반 검증을 사용하여 가독성이 좋음. 다만, 일부 테스트에서 NullPointerException 검증이 서비스 메서드 내부 방어 로직과 맞물리지 않아 실제 방어 로직 추가가 필요할 수 있음.
